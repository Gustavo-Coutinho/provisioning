version: '3'
services:
  homarr:
    container_name: homarr
    image: ghcr.io/ajnart/homarr:latest
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /home/ubuntu/homarr/configs:/app/data/configs
      - /home/ubuntu/homarr/icons:/app/public/icons
      - /home/ubuntu/homarr/data:/data
    ports:
      - '80:7575'